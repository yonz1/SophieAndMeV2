<UserControl x:Class="SophieAndMe.MVVM.View.Custom.FCCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:SophieAndMe.MVVM.View.Custom"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <WrapPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 30 0 0">
        <ComboBox
            VerticalContentAlignment="Center"
            HorizontalContentAlignment="Center"
            Margin="10,0,16,0"
            Height="50"
            Width="200"
            HorizontalAlignment="Center"
            Foreground="{StaticResource Textcolor}"
            materialDesign:ThemeAssist.Theme="Dark"
            VerticalAlignment="Center"
            materialDesign:HintAssist.Hint="Matiére"
            ItemsSource="{Binding Mat}"
            SelectedItem="{Binding SelectedMat}"
            Style="{StaticResource MaterialDesignComboBox}">
        </ComboBox>
        <ComboBox
            Margin="10,0,16,0"
            Height="50"
            Width="200"
            materialDesign:ThemeAssist.Theme="Dark"
            VerticalContentAlignment="Center"
            HorizontalContentAlignment="Center"
            HorizontalAlignment="Center"
            Foreground="{StaticResource Textcolor}"
            VerticalAlignment="Center"
            materialDesign:HintAssist.Hint="Chapitre"
            ItemsSource="{Binding Chapitre}"
            SelectedItem="{Binding SelectedChap}"
            Style="{StaticResource MaterialDesignComboBox}">
        </ComboBox>
        </WrapPanel>
    </Grid>
</UserControl>
